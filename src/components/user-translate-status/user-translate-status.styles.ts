import { makeStyles } from '@material-ui/core/styles';

const useUserTranslateStatusStyles = makeStyles((theme) => ({
    success: {
        backgroundColor: "green",
        color: "white"
    },
}));

export default useUserTranslateStatusStyles;
