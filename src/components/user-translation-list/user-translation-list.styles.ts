import { makeStyles } from '@material-ui/core/styles';

const useUserTranslationListStyles = makeStyles((theme) => ({
    container: {
        padding: "0 30px",
        marginTop: '30px'
    },
    table: {
      minWidth: "100%",
    },
}));

export default useUserTranslationListStyles;
