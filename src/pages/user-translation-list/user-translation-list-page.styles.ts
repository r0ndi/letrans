import { makeStyles } from '@material-ui/core/styles';

const useUserTranslationListPageStyles = makeStyles((theme) => ({
    title: {
        color: '#173f5f',
        fontWeight: 'bold',
        marginTop: '50px',
        marginLeft: '30px',
        fontSize: '4vw'
    },
}));

export default useUserTranslationListPageStyles;
